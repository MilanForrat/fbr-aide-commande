{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% block body %}
    <h1>test</h1>
    <main class="container">
        <section class="row justify-content-center">
            <div class="col-8 col-md-6">
                {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Ajouter le produit" class="btn btn-success float-end my-3">
                {{ form_end(form) }}
            </div>
        </section>
    </main>
    
{% endblock %}